# Sabbatical

Sabbatical is a developer-centric CLI tool for orchestrating specialized organizations of AI agents. It operates on a Client-Server architecture running locally on the developer's machine.

## Setup

```bash
poetry install
```

## Running the server

```bash
poetry run sabbatical server up
```
